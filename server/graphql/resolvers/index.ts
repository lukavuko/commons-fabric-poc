import { Query } from "./query";
import { Mutation } from "./mutation";
import {
  DateTime,
  Community,
  Event,
  Announcement,
  Comment,
  UserSubscription,
  UserEvent,
  UserRole,
  Hub,
  User,
} from "./fields";

export const resolvers = {
  DateTime,
  Query,
  Mutation,
  Community,
  Event,
  Announcement,
  Comment,
  UserSubscription,
  UserEvent,
  UserRole,
  Hub,
  User,
};
